model User{
    id String @id @default(uuid());
    name string
    email string
    password string
    createdAt Date
    editedAt Date
}

interface user {
    name:string;
    email:string;
    password:string;
}

tasks.map((task)=>{

})

forEach((task)=>{

})


let name = "sagar"

let name="sagar interview"

5 === "5"


promise.all(
    
)